# V0.1 验证记录

日期：2026-08-19

状态：离线基线已验证；尚未完成真实项目试运行，尚未接入全局强制门。

## 已验证事实

- Skill Creator 结构校验通过。
- 12 个真实工作负载夹具全部命中预期模型、推理档位和风险下限。
- 16 项 Python 回归测试全部通过，包括输入校验、模型—档位组合、本机模型缓存一致性、高风险不降档、Spark 文本编码约束和生产动作禁止自动 Ultra。
- `evaluation-fixtures.json` 通过 JSON 语法校验，所有改动通过 `git diff --check`。

## 独立前测

三个不含预期答案的独立任务均只读取 Skill 并生成路由卡，没有修改文件或执行外部动作：

| 场景 | 实际推荐 | 关键表现 |
|---|---|---|
| 已定位、单文件、可回滚的 CSS 间距修复，优先额度 | `gpt-5.3-codex-spark` · `high` | 正确使用独立池并等待确认 |
| 真实支付、隐私数据、数据库迁移的生产部署，通用额度约剩 15% | `gpt-5.6-sol` · `xhigh` | 没有因额度不足降档；先要求备份、演练、回滚与授权证据 |
| 8 个可独立拆分仓库的只读架构与安全审计，优先质量 | `gpt-5.6-sol` · `ultra` | 正确把 Ultra 解释为多 Agent 并行，而非普通高推理档 |

## 尚未验证

- 尚无 7 天或 10 个真实任务的用户接受率、返工轮数、完成时间和实际额度数据。
- 尚未证明路由建议能在真实项目中降低总返工或额度浪费。
- 尚未验证全局触发不会过度打断日常连续工作。

## 下一阶段通过标准

- 至少 10 个真实任务，或连续 7 天试运行；
- 高风险低配错误为 0；
- 用户接受推荐比例至少 80%；
- 同类任务在事实不变时路由稳定；
- 同一阶段不重复弹出确认；
- 有证据表明返工或无效额度消耗下降。

达到以上门槛并经用户再次确认后，才考虑把 Skill 接入全局 `AGENTS.md` 与项目启动脚本。
